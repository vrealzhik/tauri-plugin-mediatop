# Tauri Plugin mediatop

add to build.gradle.kts in tauri
allprojects {
    repositories {
        google()
        mavenCentral()
        maven("https://artifactory.appodeal.com/appodeal-public")
    }
}